# Server
PORT=3001
NODE_ENV=development

# Database
DB_PATH=./data/homelab.db

# Logging (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# Security
# Restrict CORS to specific origins (comma-separated). Leave unset to allow all origins.
# CORS_ORIGIN=http://localhost:3000,http://192.168.1.100:1781

# Host system access (used when running in Docker)
# Path to host /proc filesystem mount
HOST_PROC=/proc
